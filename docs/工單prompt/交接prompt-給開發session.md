# 交接 prompt — 貼給開發 session

> 使用方式：把下面 `---` 之間的整段複製貼給新的開發 session。

---

你是 Whisper Pro 的開發 session。專案在 `/Users/jerrychen/project/Claude_code`
（**不是** MeetingNotes，那是姊妹產品，這次不要碰它）。

## 這次要做什麼

執行工單 **T01**：`docs/工單prompt/T01-靜音chunk吃掉近七成算力.md`

**先完整讀這三份，再動手**（順序照這個）：
1. `docs/工單prompt/T01-靜音chunk吃掉近七成算力.md` — 你要執行的工單
2. `docs/優化策略-更快更準-2026-09-05.md` — 這張工單的來由與完整證據
3. `CLAUDE.md` — 專案交接手冊
   ⚠️ **這份文件已知落後於程式碼**（例如它寫主模型是 large-v3-turbo，
   實際已是 Qwen3-ASR，佔 93%）。看架構思路可以，**版本號與現況描述一律自己驗證**。

## 一句話背景

**69.2% 的模型算力，花在轉錄錄音中的停頓，轉出來的結果全部被丟掉**
（2,486 筆字典 dump，累計 390.5 分鐘推論）。根因是 `transcriber.py:941` 的
Silero VAD 閘有 `duration <= 4.0` 限制，而串流 chunk 是 10–12 秒，永遠繞過它。

## 🔴 這次只做 T01 的〔A〕

〔A〕是「**加量測、不改任何行為**」，然後**需要跑一週日常使用**才有資料。
所以：

- **這個 session 只做〔A〕，做完就停。**
- 〔B〕（訂門檻）和〔C〕（實作 gate）**必須等一週後、有資料了、另開 session 才做**。
- 不要因為「順便」就把 gate 一起寫了。**沒有資料就訂門檻 = 猜。**

## 🚨 已經被推翻的方向，不要再試

規劃 session 已經用 2,424 筆 dump vs 2,444 筆真語音 chunk 做過離線分離測試：

| 特徵 | 零誤擋下的攔截率 |
|---|---|
| `peak` | **0.0%** |
| `rms` | **0.0%** |

分布幾乎完全重疊（dump 最大 peak 1.1712，真語音最小 peak 0.0443）。

**所以：不要調 `_MIN_RMS`（`transcriber.py:318`），不要用音量做門檻。**
如果你覺得「再試一次音量說不定可以」——先看策略文件 §4-2 的完整數據再說。

## 紀律（違反就重做）

1. **每一句關於程式現況的話都要對得到真實 `檔案:行號`，親自開檔驗證。禁止臆造。**
   工單裡的行號是 2026-09-05 驗的，**你要自己再確認一次**，程式碼可能動過。
   不確定就寫「待查」，不要編一個看似合理的數字。
2. **外科手術式修改**：只碰必須碰的。不要順手改旁邊的程式碼、註解、排版。
   交付前看 `git diff`，確認每一行改動都能追溯到 T01 的要求。
3. **隱私**：真實錄音、逐字稿內容一律不進版控、不進 log、不印到 stdout。
   要引用測試素材用代號（`SAMPLE-01`）。
4. **驗收條件要真的跑**，不要「看起來對了」就算過。工單 §四〔A〕有可執行的指令。
   特別是 **A2：行為零變化**——同一份 golden set 跑 `eval_runner.py`，
   輸出必須跟改動前 **byte-identical**。
5. **每建一把尺，都要故意餵一個「應該失敗」的輸入**，確認它真的擋得住。
   永遠回 PASS 的檢查等於沒有檢查。
6. **commit 衛生**：提交前先 `git status`，確認要提交的東西跟訊息相符。
   **不要 `git add -A`**。一個 commit 一件事。
7. **開工前先 `git pull`**，並看 `docs/工作紀錄/` 最近三則。
8. 完成後在 `docs/工作紀錄/` 新增交接紀錄（`YYYY-MM-DD_T01-A-*.md`），
   **跟程式碼一起提交**。五個欄位裡「意外發現」與「留給下一個人的坑」最重要。

## 溝通

使用者是 PM／PjM，不是工程師。**用白話講，結論先講。**
技術過程（版號、指令、內部編號）壓縮成一兩句；他說「講細節」才展開。
所有文件與 commit 訊息本文用繁體中文（`feat:` `fix:` 這類前綴保留英文）。

## 開始前先回報

動手之前，先跟使用者確認這三件事：
1. 你複核工單行號的結果（有沒有跟 2026-09-05 記錄的不一致）
2. 你打算加哪幾個欄位、寫在哪個檔案的哪一段
3. 你打算怎麼驗「行為零變化」

**他同意之後才開始改。**

---

## （備用）T02 的交接 prompt

T02 是「建立走真實路徑的準確度尺」，**前置條件是使用者要先錄一份真人參考素材**。
素材沒到位不要開工——會變成「先用 TTS 試試看」，而 TTS 量準確度已知會差 5 倍
（MeetingNotes 實測：TTS 42.7% vs 真人 7.8%）。

工單在 `docs/工單prompt/T02-建立走真實路徑的準確度尺.md`。
確認素材到位後，把上面那段紀律原封不動沿用，把「執行工單 T01」換成 T02 即可。
