# System dependencies (install before pip):
#   brew install portaudio ffmpeg

customtkinter==5.2.2
sounddevice==0.5.1
numpy==2.2.4
faster-whisper==1.2.1
pyperclip==1.9.0
pynput==1.8.1
requests==2.32.3

# Apple Silicon GPU acceleration (auto-used on arm64 Macs)
mlx==0.31.1
mlx-whisper==0.4.3
webrtcvad==2.0.10

# v2.14.0 Qwen3-ASR backend（Apple Silicon MLX、中文 acoustic 強於 Whisper turbo）
# 與 mlx-whisper 並存、user 可在設定切；非 Apple Silicon 自動 fallback 錯誤訊息
mlx-qwen3-asr==0.3.5
# v2.14.0 簡↔繁字體轉換（Qwen3-ASR 預設輸出簡體、用 s2twp 轉繁體 + 台灣慣用語）
OpenCC==1.3.1
