# System dependencies (install before pip):
#   brew install portaudio ffmpeg

customtkinter==5.2.2
sounddevice==0.5.1
numpy==2.2.4
faster-whisper==1.2.1
pyperclip==1.9.0
pynput==1.8.1
requests==2.32.3

# Apple Silicon GPU acceleration (auto-used on arm64 Macs)
mlx==0.31.1
mlx-whisper==0.4.3
webrtcvad==2.0.10

# v2.14.0 Qwen3-ASR backend（Apple Silicon MLX、中文 acoustic 強於 Whisper turbo）
# 與 mlx-whisper 並存、user 可在設定切；非 Apple Silicon 自動 fallback 錯誤訊息
mlx-qwen3-asr==0.3.5
# v2.14.0 簡↔繁字體轉換（Qwen3-ASR 預設輸出簡體、用 s2twp 轉繁體 + 台灣慣用語）
OpenCC==1.3.1

# v2.18.0 雲端 polish backend（Google Vertex AI Gemini、選用）
# 認證走 ADC：跑一次 `gcloud auth application-default login` 即可
google-genai>=1.0.0

# v2.19.x Silero VAD v5 神經前置過濾（擋鍵盤/背景音/雜音、抑制 ASR 幻覺）
# 放在 RMS gate 之後、ASR 之前；用神經網路判斷音檔是否含人聲
silero-vad>=5.0.0

# v2.19.x Pinyin guard（拼音 fuzzy match 修中文 ASR 同音/近音誤辨）
pypinyin>=0.50.0

# v2.20.3 N5：keepalive ping 記憶體 snapshot 用（RSS / VM size）
psutil>=5.9.0
